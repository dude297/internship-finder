# Base URL of the FastAPI backend. Exposed to the browser (all VITE_* vars are public),
# so never put secrets here. `npm run dev` falls back to http://localhost:8000 if unset;
# production builds must set it explicitly.
VITE_API_BASE_URL=http://localhost:8000
