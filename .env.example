# Environment variables for the Personal Internship Finder.
#
# No environment variables are required yet. Variables will be added here,
# with placeholder values only, as features require them.
#
# Copy to .env (or the framework's equivalent) and fill in real values locally.
# Never commit real secrets. Never expose server-only/service-role keys to the browser.
